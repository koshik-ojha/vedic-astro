# Frontend (Next.js) — Free MVP

## Quickstart
```bash
cd frontend
npm install
cp .env.local.example .env.local
npm run dev
```

Set `.env.local`:
- `NEXT_PUBLIC_BACKEND_URL=http://localhost:8000`

Open http://localhost:3000
